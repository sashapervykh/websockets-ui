interface IncRegMessage {
  type: "reg";
  data: { name: string; password: string };
  id: number;
}

interface IncRegMessage {
  type: "reg";
  data: { name: string; password: string };
  id: number;
}
